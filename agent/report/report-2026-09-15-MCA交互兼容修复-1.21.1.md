# MCA 交互兼容修复执行简报（1.21.1 NeoForge）

## 结果

- 新增仅面向 MCA 的 `EntityInteractSpecific` 高优先级监听，并在模组入口注册。
- 仅当 `mca` 已加载、目标实体注册命名空间为 `mca`、手持物为 Trade Tweaks 村民工具时提前执行已有物品逻辑。
- 绿宝石法杖继续要求潜行、存活且未睡眠的村民；未改变原有模式、消耗、冷却或服务端判定。
- 事件仅在已有物品逻辑返回非 `PASS` 时取消，从而避免 MCA 抢先消费本次右键。
- 未引入 MCA 编译或运行依赖，未修改原有 `EntityInteract` 处理器。

## 稳定性边界

- 未安装 MCA：首个条件立即返回，原交互链保持不变。
- 非 MCA 实体：按注册命名空间立即返回，原交互链保持不变。
- 非 Trade Tweaks 村民工具：立即返回，MCA 自身右键交互保持不变。
- 代码复用各物品现有 `interactLivingEntity`，没有复制玩法规则。

## 验证

- 使用 Java 21 对兼容处理器及修改后的模组入口进行了独立编译，编译通过。
- GitHub Actions 完整 Gradle `build` 成功（run `34866771955`）。
- 将测试 jar 与修改前生产 jar 逐条目比较：仅新增 `McaInteractionHandler.class`、更新 `TradeTweaks.class` 注册入口，其余 136 个文件内容哈希全部不变。
- `git diff --check` 通过。
- 本机 Gradle 客户端因系统 Java NIO 回环连接异常无法连接单次构建进程；此为代码编译前发生的主机环境故障，未伪报为项目测试通过。

## 测试环境

已向 `D:\0game\HMCL\.minecraft\versions\1.21.1-NeoForge\mods` 增量放入：

- `tradetweaks-1.21.1-2.1.2-mca-test.jar`
- `mca-neoforge-7.7.32+1.21.1.jar`

目标实例为 NeoForge 21.1.250。MCA 7.7.32 的元数据不声明额外必需模组。原目录中的 Awakened、Curios、OneKick 和 PlayerAnimationLib 文件均原样保留。

## 未完成项与风险

- 仍需由用户从 HMCL 启动真实客户端，完成 MCA 村民交互及无 MCA 回归测试。
